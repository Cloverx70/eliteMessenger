import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["elitemessenger.s3.eu-north-1.amazonaws.com", "api.dicebear.com"],
  },
  /* config options here */
};

export default nextConfig;
